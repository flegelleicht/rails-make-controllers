# rails-make-controllers

## Run

```
docker-compose up -d
docker-compose exec api bundle exec rails s --binding=0.0.0.0
```

## Todo

- [ ] Remove warning: /project/api/config/initializers/controllers.rb:8: warning: class variable access from toplevel
